# Aurelens 0.7.0 (build 20) 真机测试与材料

日期：2026-09-14（美国太平洋时间）。**本轮发现发布阻塞：真机模拟购买完成后，应用没有获得付费权限。不能将 build 20 标记为全部测试通过或可提交审核。**

## 环境与结果

iPhone 17 Pro Max，iOS 27.0 (24A435)；Xcode 26.6。iPad Pro 13-inch (M5) 使用 iPadOS Simulator 26.5。购买全部使用 Xcode 本地 StoreKit 配置，支付弹窗明确显示仅用于测试、不会收费；没有真实扣款，也没有发送收费 AI 请求。

| 范围 | 最终结果 | 本地证据（build/device-test20-20260914） |
|---|---|---|
| 真机核心测试 | 62 通过，6 失败，0 跳过 | iPhone-core-isolated-final.xcresult |
| 真机中英文参考页面采集 | 1 通过，16 张截图 | iPhone-reference-wireless.xcresult |
| 真机功能检查 | 5 通过 | iPhone-functional.xcresult |
| 真机模拟购买及付费解锁 | 1 失败 | iPhone-purchase-capture-final.xcresult |
| 真机支付弹窗取消 | 1 通过 | iPhone-cancel-capture-final.xcresult |
| 真机恢复后 Release 页面检查 | 1 通过 | iPhone-final-state.xcresult |
| iPad 中英文参考采集 | 1 通过，16 张截图 | iPad-reference-final.xcresult |
| iPad StoreKit 补测 | 串行重跑 9 通过 | iPad-storekit-serial-final.xcresult |
| 原有应用数据恢复 | 1 通过，另经字节比对确认 | state-restored-final.xcresult / restored-data-verification.json |

真机功能覆盖：风险提示拒绝与重新同意；后台切换、关闭重开后试用截止时间不变；商品不可用时重试、恢复购买入口和法律链接；全部十个周期、横纵轴缩放、自动纵轴及回到最新；持仓录入测试值后撤销回原值；暂停与恢复前台刷新。未进行外部 AI 付费请求或真实交易。

## 付费解锁失败

本地月度商品金额为 USD 1.00，类型为自动续期订阅，周期为一个月、无介绍优惠。模拟购买生成已购买交易，其到期日是一个月后，`Transaction.latest(for:)` 返回已验证、未撤销的月度交易。但是 `Transaction.currentEntitlements` 和按商品查询的有效权益序列为空，订阅状态列表也为空，应用继续显示免费试用，未出现“订阅已生效”。该现象在实际购买界面中复现，并已保存截图与录屏。

关闭所有模拟器、重新打开 Xcode、明确选择真机并用包含月度配置的 Run 会话启动后，完整核心测试仍为 62 通过 / 6 失败。清除模拟错误、重新创建本地测试商品/分组的诊断尝试也未解决；诊断用配置已恢复，未修改发行包或降低交易验证要求。原因尚未确定，不能断言只是测试工具问题，也不能据此声称线上购买已经通过。

失败场景包括：家长批准后的解锁、关闭续期/重新续期、月度价格与续期状态的综合测试、购买/恢复/到期、退款测试，以及取消错误注入测试。前五项因拿不到有效付费权益而失败，无法宣称真机恢复、到期或退款链路已完整验证。

取消错误注入另有独立现象：iOS 27 将 `.userCancelled` 注入返回为 ASDServerErrorDomain 907，应用显示失败而非取消。**真实操作 Xcode 测试支付弹窗的关闭按钮已通过**：显示“Purchase cancelled.”、没有已购买交易、保留原试用。两种结果分别记录，未用界面通过掩盖核心断言失败。

iPad 本轮先运行 9 项 StoreKit 测试，8 通过、1 续期场景失败；停止其他 StoreKit 测试并重启专用模拟器后，完整 9 项串行重跑全部通过。覆盖价格/周期/自动续期、购买恢复到期、取消续期后保留有效期及续期延长、退款、家长批准/拒绝、取消与错误、商品加载恢复、无效签名拒绝。此前全平台 203 核心通过与 8 个界面场景通过属于[较早的模拟器/macOS 验证阶段](VERIFICATION-0.7.0-build20.md)，不能覆盖本轮真机失败。

## 材料

桌面：`截图/build20/真机测试-20260914`。

- 32 张参考截图，沿用 build 16 的 iPhone/iPad、中英文文件对应关系，并增加订阅页。
- iPhone：1320 × 2868 原始 PNG；iPad：2064 × 2752 原始 PNG。
- 两条参考视频：iPhone 136 秒、iPad 153 秒，H.264、原始分辨率、无音轨。
- `功能检查-真机`：10 张截图、5 条录屏；`模拟订阅-真机`：4 张截图、2 条录屏，包括购买未解锁的失败现场。
- 所有截图均为 XCTest 原生附件的原样副本。视频仅截取时间区间，没有替换数据或重新绘制页面。完整原片、来源映射和校验记录保留在本地 `采集记录`；早期失败/中断诊断保留在忽略的 build 目录。
- 订阅截图中的 USD 1.00 来自本地测试商店，不能用来证明 App Store Connect 线上商品已经配置或审核通过。iPad 订阅页同样属于模拟器本地 StoreKit 环境。
- 公布的材料只包含检查过的应用画面、测试说明和校验值，不包含用户偏好备份、聊天数据文件、设备标识、桌面截图、完整调试日志或原片中的手机桌面。

## 恢复与构建

最终在手机上原位安装原有 **0.7.0 / build 20 开发签名 Release 包**。`settings.v1`、`position.v1`、`chat.v1` 与本轮开始前的备份逐字节相同；试用截止时间与初始采集一致，未重置 Keychain 试用记录。清除模拟交易和错误，移除本轮新增的两个测试 runner，保留原有应用。专用模拟器已关闭。

本轮只补充采集工具、测试诊断与文档，没有修改应用生产代码、商品标识、价格或订阅规则。临时 Xcode 本地签名配置与诊断商品配置均已还原。项目结构、Release 包审计、原始安装包校验继续通过。

- 开发安装包 SHA-256：`22d5c4e5ac253c8ba984289995a980a07ea7f4ffc0b4bee51085282bc212c759`。
- App Store IPA SHA-256：`da8e30d2b25614e2bb6798b18ab9d6682647a5b0927a595aa9e9846d4229cb93`。
- 应用实现提交：`dbcfa94b3273bf8276f860ad07807c1fbc9ae89e`；本轮后续提交仅为测试、采集和记录。

## App Store Connect 与发布结论

同一个 build 20 已于 2026-09-14 16:56:13 UTC [成功上传 App Store Connect](APPSTORE-UPLOAD-0.7.0-build20.md)，回执显示进入 Apple 处理流程。安装包哈希未改变，因此本轮没有重复上传同一构建号。后续处理完成状态尚未核实，未提交 App Review。

**在解决并复测真机付费解锁问题，以及核实线上月度商品/价格与 Apple 沙盒交易前，不应提交审核或对外宣称订阅已验证可用。** GitHub 保持预发行状态，同时提供现有发行包、源码和本次真实测试结论。历史构建、文件名、标签和旧截图均保留。
